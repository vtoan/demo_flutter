import 'package:flutter/material.dart';
class NotificationFragmentWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Notification', style: TextStyle(fontSize: 24),),
    );
  }
}

